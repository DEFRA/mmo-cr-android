source "https://rubygems.org"

# Pins Fastlane for the CI build/test lanes and the later Play release lanes.
# Run `bundle install` to regenerate Gemfile.lock after changing this file.
gem "fastlane"
